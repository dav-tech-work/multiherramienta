<header class="header">
  <div class="contenedor-header">
    
    <!-- 🔷 Logo / Nombre del Proyecto -->
    <a href="/" class="logo">SysTools</a>

    <!-- 🔧 Navegación contextual -->
    <nav class="menu-principal">
      <!-- 🌍 Siempre disponibles (zona pública) -->
      <a href="/ip">Ver IP</a>
      <a href="/escaner-red">Escanear Red</a>
      <a href="/whois">WHOIS</a>
      <a href="/dns">DNS</a>
    
      <!-- 🛠 Zona privada (si está logueado) -->
      <% if (typeof usuario !== 'undefined' && usuario) { %>
        <a href="/panel">Dashboard</a>
        <a href="/herramientas/nmap">Nmap</a>
        <a href="/herramientas/nikto">Nikto</a>
        <a href="/reportes">Reportes</a>
      <% } %>
    
      <!-- 🛡 Zona admin -->
      <% if (typeof usuario !== 'undefined' && usuario?.rol === 'admin') { %>
        <a href="/admin/panel">Panel Admin</a>
        <a href="/admin/usuarios">Usuarios</a>
        <a href="/admin/logs">Logs</a>
        <a href="/admin/config">Configuración</a>
      <% } %>
    </nav>

    <!-- ⚙️ Controles del usuario -->
    <div class="acciones-usuario">

      <!-- 🌐 Selector de idioma -->
      <button id="selector-idioma" title="Idioma" aria-label="Seleccionar idioma">🌐</button>

      <!-- 🌓 Botón tema oscuro/claro -->
      <button id="toggle-tema" title="Tema" aria-label="Cambiar tema">🌓</button>

      <!-- 👤 Autenticación -->
      <% if (typeof usuario !== 'undefined' && usuario) { %>
        <a href="<%= usuario.rol === 'admin' ? '/admin/panel' : '/panel' %>" class="btn-usuario">
          <%= usuario.nombre %>
        </a>
        <form action="/logout" method="POST" class="logout-form">
          <button type="submit" title="Cerrar sesión" class="btn-logout">⏻</button>
        </form>
      <% } else { %>
        <a href="/login" class="btn-login">Iniciar sesión</a>
      <% } %>

    </div>
  </div>
</header>
